// DATAtypes of the Dart

void main() {
  int x = 1;
  print(x);

  dynamic d1 = "Hello Bidhu";

  print(d1);

  // KeySwORED in the Dart

  var v1 = 156;
  print(v1);

  v1 = 256;

  print(v1);

  // Var are also used to stored the data and also used to stored the data and reassing the values

  //Second Keyboards  -- const Keywords

  const k1 = 15;
  print(k1);

  // k1 = 89; // Where  the Const are compile tuime veriofication to make the data are constant then we cant be reassign the values in the const keywords
  print(k1);

  // Third is ----  final Keywords

  final f1 = 134;
  print(f1);

  // Final Keywords are also used to stored the data but it is the run time verificationof the code which is used to store d the latest data of the application but we cant reassign the values

  final date = DateTime.now();
  print(date);

  //const date1= DateTime.now(); // Here  is the compile time verification he gives the error of because itss is the compiler time verifications

  //print(date1);

  // So this is the keywords to stored the data from  the compile times in the Appppllicatiosn
  // best mannner to make the variable to stored the data in to camelcase.

  // This is the Basic Datatype to stored the data

  
}
